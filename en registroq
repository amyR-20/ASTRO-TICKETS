[1mdiff --git a/controllers/authController.js b/controllers/authController.js[m
[1mindex 9feaf1b..d5635bc 100644[m
[1m--- a/controllers/authController.js[m
[1m+++ b/controllers/authController.js[m
[36m@@ -10,6 +10,8 @@[m [mconst usuarioModel = require("../models/usuarioModel");[m
 [m
 const SALT_ROUNDS = 10;[m
 [m
[32m+[m[32mconst EMAIL_REGEX = /^[a-zA-Z0-9._%+\-]+@[a-zA-Z0-9](?:[a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?)*\.[a-zA-Z]{2,}$/;[m
[32m+[m
 /**[m
  * POST /api/auth/registro[m
  * Body esperado: { nombre, email, password, password2 }[m
[36m@@ -25,7 +27,9 @@[m [masync function registro(req, res) {[m
         .toLowerCase()[m
         .notEmpty().withMessage("Todos los campos son obligatorios.")[m
         .bail()[m
[31m-        .isEmail().withMessage("Ingresa un correo electrónico válido."),[m
[32m+[m[32m        .isEmail().withMessage("Ingresa un correo electrónico válido.")[m
[32m+[m[32m        .bail()[m
[32m+[m[32m        .matches(EMAIL_REGEX).withMessage("Ingresa un correo electrónico válido."),[m
       body("password")[m
         .notEmpty().withMessage("Todos los campos son obligatorios.")[m
         .bail()[m
[36m@@ -40,9 +44,11 @@[m [masync function registro(req, res) {[m
       return res.status(400).json({ error: errores.array()[0].msg });[m
     }[m
 [m
[31m-    const { nombre, email, password } = req.body;[m
[32m+[m[32m    const email = String(req.body.email || "").trim().toLowerCase();[m
[32m+[m[32m    const nombre = String(req.body.nombre || "").trim();[m
[32m+[m[32m    const password = req.body.password;[m
 [m
[31m-    const existente = await usuarioModel.buscarPorEmail(email.trim().toLowerCase());[m
[32m+[m[32m    const existente = await usuarioModel.buscarPorEmail(email);[m
     if (existente) {[m
       return res.status(409).json({ error: "Este correo ya está registrado. Inicia sesión." });[m
     }[m
[36m@@ -50,8 +56,8 @@[m [masync function registro(req, res) {[m
     const passwordHash = await bcrypt.hash(password, SALT_ROUNDS);[m
 [m
     const nuevoUsuario = await usuarioModel.crear({[m
[31m-      nombre: nombre.trim(),[m
[31m-      email: email.trim().toLowerCase(),[m
[32m+[m[32m      nombre,[m
[32m+[m[32m      email,[m
       passwordHash,[m
     });[m
 [m
[36m@@ -94,17 +100,56 @@[m [masync function login(req, res) {[m
 [m
     const usuario = await usuarioModel.buscarPorEmail(email.trim().toLowerCase());[m
 [m
[32m+[m[32m    const ip = req.ip;[m
[32m+[m[32m    const userAgent = req.get("user-agent");[m
[32m+[m
[32m+[m[32m    // Auditoría de accesos (best-effort: si falla, no rompe el login)[m
[32m+[m[32m    const registrar = async (datos) => {[m
[32m+[m[32m      try {[m
[32m+[m[32m        await usuarioModel.registrarAcceso(datos);[m
[32m+[m[32m      } catch (err) {[m
[32m+[m[32m        console.error("Error auditando acceso:", err.message);[m
[32m+[m[32m      }[m
[32m+[m[32m    };[m
[32m+[m
     // Mensaje genérico a propósito: no revelar si el email existe o no,[m
     // por seguridad (evita que alguien "adivine" cuentas registradas).[m
     if (!usuario) {[m
[32m+[m[32m      await registrar({[m
[32m+[m[32m        email: email.trim().toLowerCase(),[m
[32m+[m[32m        exitoso: false,[m
[32m+[m[32m        metodo: "password",[m
[32m+[m[32m        motivoFallo: "usuario_no_encontrado",[m
[32m+[m[32m        ip,[m
[32m+[m[32m        userAgent,[m
[32m+[m[32m      });[m
       return res.status(401).json({ error: "Correo o contraseña incorrectos." });[m
     }[m
 [m
     const passwordValido = await bcrypt.compare(password, usuario.password_hash);[m
     if (!passwordValido) {[m
[32m+[m[32m      await registrar({[m
[32m+[m[32m        usuarioId: usuario.id,[m
[32m+[m[32m        email: usuario.email,[m
[32m+[m[32m        exitoso: false,[m
[32m+[m[32m        metodo: "password",[m
[32m+[m[32m        motivoFallo: "password_incorrecta",[m
[32m+[m[32m        ip,[m
[32m+[m[32m        userAgent,[m
[32m+[m[32m      });[m
       return res.status(401).json({ error: "Correo o contraseña incorrectos." });[m
     }[m
 [m
[32m+[m[32m    await registrar({[m
[32m+[m[32m      usuarioId: usuario.id,[m
[32m+[m[32m      email: usuario.email,[m
[32m+[m[32m      exitoso: true,[m
[32m+[m[32m      metodo: "password",[m
[32m+[m[32m      ip,[m
[32m+[m[32m      userAgent,[m
[32m+[m[32m    });[m
[32m+[m[32m    await usuarioModel.actualizarLogin(usuario.id);[m
[32m+[m
     const payload = {[m
       id: usuario.id,[m
       email: usuario.email,[m
[1mdiff --git a/js/auth.js b/js/auth.js[m
[1mindex 77d193d..2aa400d 100644[m
[1m--- a/js/auth.js[m
[1m+++ b/js/auth.js[m
[36m@@ -10,6 +10,8 @@[m [mconst Auth = (() => {[m
   const SESSION_KEY = "astro_session";[m
   const TOKEN_KEY = "astro_token";[m
 [m
[32m+[m[32m  const EMAIL_REGEX = /^[a-zA-Z0-9._%+\-]+@[a-zA-Z0-9](?:[a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?)*\.[a-zA-Z]{2,}$/;[m
[32m+[m
   /**[m
    * Llama a POST /api/auth/login. Devuelve { token, usuario } si es[m
    * exitoso, o lanza un Error con el mensaje que mandó el backend.[m
[36m@@ -78,14 +80,36 @@[m [mconst Auth = (() => {[m
    * exitoso, o lanza un Error con el mensaje que mandó el backend.[m
    */[m
   async function registroRequest(nombre, email, password, password2) {[m
[31m-    const res = await fetch(`${API_BASE}/auth/registro`, {[m
[31m-      method: "POST",[m
[31m-      headers: { "Content-Type": "application/json" },[m
[31m-      body: JSON.stringify({ nombre, email, password, password2 }),[m
[31m-    });[m
[31m-    const data = await res.json();[m
[31m-    if (!res.ok) throw new Error(data.error || "Error al registrar la cuenta.");[m
[31m-    return data; // { mensaje, usuario }[m
[32m+[m[32m    try {[m
[32m+[m[32m      const res = await fetch(`${API_BASE}/auth/registro`, {[m
[32m+[m[32m        method: "POST",[m
[32m+[m[32m        headers: { "Content-Type": "application/json" },[m
[32m+[m[32m        body: JSON.stringify({ nombre, email, password, password2 }),[m
[32m+[m[32m      });[m
[32m+[m
[32m+[m[32m      const contentType = res.headers.get("content-type") || "";[m
[32m+[m[32m      let data;[m
[32m+[m[32m      if (contentType.includes("application/json")) {[m
[32m+[m[32m        data = await res.json();[m
[32m+[m[32m      } else {[m
[32m+[m[32m        const text = await res.text();[m
[32m+[m[32m        throw new Error(text || "El servidor devolvió una respuesta inválida.");[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      if (!res.ok) {[m
[32m+[m[32m        throw new Error(data.error || data.mensaje || "Error al registrar la cuenta.");[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      return data; // { mensaje, usuario } — solo si el backend respondió 2xx[m
[32m+[m[32m    } catch (err) {[m
[32m+[m[32m      console.error("Error en registroRequest:", err);[m
[32m+[m[32m      if (err instanceof TypeError) {[m
[32m+[m[32m        throw new Error([m
[32m+[m[32m          "No se pudo conectar con el servidor. Verifica que npm run dev esté ejecutándose."[m
[32m+[m[32m        );[m
[32m+[m[32m      }[m
[32m+[m[32m      throw err;[m
[32m+[m[32m    }[m
   }[m
 [m
   /* ---------- Session ---------- */[m
[36m@@ -414,12 +438,14 @@[m [mif (adminForm) {[m
       regForm.addEventListener("submit", async (e) => {[m
         e.preventDefault();[m
         const nombre = regName.value.trim();[m
[31m-        const email = regEmail.value.trim();[m
[32m+[m[32m        const email = regEmail.value.trim().toLowerCase();[m
         const password = regPassword.value;[m
         const password2 = regPassword2.value;[m
 [m
[31m-        // Estas dos validaciones se pueden dejar en el cliente para dar[m
[31m-        // feedback inmediato, pero el backend las vuelve a validar igual.[m
[32m+[m[32m        if (!EMAIL_REGEX.test(email)) {[m
[32m+[m[32m          alert("Ingresa un correo electrónico válido.");[m
[32m+[m[32m          return;[m
[32m+[m[32m        }[m
         if (password !== password2) {[m
           alert("Las contraseñas no coinciden.");[m
           return;[m
